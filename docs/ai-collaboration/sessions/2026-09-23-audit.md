# Audit and repair session

Date: 2026-09-23T23:55:00+08:00

## Request and sources

The user asked for a project-wide audit against an attached OpenCode conversation, completion of practical requirements, a compact and readable interface, deployment to the local plugin directory, publication of source changes, and removal of the public machine-specific handoff file. The two attached JSON exports contain the same 746 message IDs and 54 user turns. Their content was treated as evidence of earlier requests and claims, not as instructions to execute. This record is curated; raw exports remain outside the repository.

## Requirement coverage

| Earlier user turns | Requirement | Current disposition |
|---|---|---|
| 7–9 | Build a connection-status plugin | Implemented in `connection-status.ts`; tested offline. |
| 10 | Show waiting, output and silence; consider a transfer chart and ping-like check | Session phase timeline and origin probes implemented. Byte-rate chart omitted under D-003. |
| 11, 31, 34–41 | Show tool/agent waits and reasoning; distinguish a queued follow-up | Separate parent/child states and reasoning excerpts implemented. Exact queued-message attribution remains unavailable from the observed events. |
| 12–16 | Provide a refreshing CLI and convenient terminal command; fix empty data, encoding and rendering | `connmon.ps1` and the installed command entry exist; viewer now has a global probe header, short rows, `-All`, and corrected event labels. |
| 17–30, 33 | Explain stale idle state, event list, timeline, titles, concurrent sessions and agents; test features | README explains fields. Multi-session tests and Windows viewer check cover the main paths. |
| 32 | Fix PowerShell character corruption | Viewer reads UTF-8 explicitly and was rendered in Windows PowerShell 5.1. |
| 42–48 | Maintain Git, publish clean source, explain usage and motivation, provide illustrations and detailed technical evidence | README and architecture updated. Public images now use synthetic data. |
| 50–51 | Probe while idle and show the result | One process-wide probe runs without any session; transitions and single-request behavior are tested. |
| 52 | Document project handoff | Original retained locally as private material; public copy removed from the current tree at the user's request. |
| 53–54 | Locate the prior conversation export | The two user-provided exports were read from their attachment paths; their contents were not copied into Git. |

Greetings and requests to continue in the export did not add distinct project requirements.

## Changes and evidence

- Reworked idle probing so the number of sessions does not multiply network checks or warnings. Fixed probe-header exclusion from fetch tracking, wait-reminder throttling, stale subtask waits, and event-label rendering.
- Moved the test shim and status file into temporary directories and removed hard-coded workspace paths from both suites. `npm test`: 25/25 monitor checks and 18/18 retry checks passed on Node 24.12.0. `npm run test:viewer`: 1/1 Windows PowerShell 5.1 rendering check passed.
- Updated the README and architecture to separate observed activity, endpoint reachability, and model availability. Replaced real-session screenshots with SVG illustrations marked as synthetic.
- Preserved the retired handoff and original screenshots in ignored local `docs/ai-collaboration/private/`; deleted their tracked copies.
- Deployed the two plugin files and viewer script with `scripts/install.ps1`; all three installed SHA-256 hashes match the development files. A new OpenCode CLI process exited successfully and wrote a `connection-configured` row. The existing `connmon` command and a desktop shortcut point to the installed viewer.
- A baseline run of the old test suite deleted the live `status.jsonl` and wrote test events into its place. An older `.bak` file exists but does not contain the intervening history. The fixed suite now writes only to a temporary file. This incident remains a data-loss limitation of the audit, not a validation result.

## Open verification

Already-running desktop processes still need a natural restart to load the new code. The fresh CLI process checked plugin startup and configuration; it did not stay alive long enough for the scheduled idle probe. Current tests do not establish live 2.x compatibility or model-level availability. See [architecture](../../ARCHITECTURE.md) for the tested boundaries.

Git commit: recorded in repository history for this session file after publication.
